# Decision Log

| Date | Decision | Rationale | Owner | Ticket |
| --- | --- | --- | --- | --- |
| 2026-01-09 | Zion UI aesthetic clinical/technical feeling is brand-aligned and intentional | Client confirmed that clinical, stark aesthetic perfectly aligns with data-driven efficiency and results philosophy. The technical appearance expresses core brand values. | Client / Implementation Engineer | Research Phase |
| 2026-01-09 | Portfolio content will be primarily image-based | Images work well with minimal aesthetic. Not a concern for content integration. | Client | Research Phase |
| 2026-01-09 | Symbol-based navigation accessibility to be handled by client | Client will manage strategy for symbol-based navigation accessibility. Implementation Engineer aware but not responsible for this aspect. | Client | Research Phase |
| 2026-01-09 | Particle effects are a core requirement for the UI system | Client specified that particle effects must be integrated into the design system as a primary feature, not optional enhancement. | Client / Implementation Engineer | Research Phase |
| 2026-01-09 | Recommended tech stack: Svelte + GSAP + tsParticles + Vite | Based on performance research, Svelte offers best performance (3x faster updates than React), GSAP provides industry-standard animation capabilities ideal for SVG line work, tsParticles offers highly customizable particle effects with framework support, and Vite provides fastest build tooling. Alternative: React + Framer Motion for ecosystem maturity. | Implementation Engineer | Research Phase |
| 2026-01-09 | WebGL-based rendering for particle effects | Research shows WebGL significantly outperforms Canvas 2D for 1000+ particles and enables advanced holographic effects. tsParticles supports WebGL mode, PixiJS for extreme performance needs (100K+ particles), Three.js for true 3D holographic depth. | Implementation Engineer | Research Phase |
| 2026-04-29 | Scrap all expansion; reduce to holding page (crest + Contact button) | Multiple visual directions had drifted into generic territory; resetting to a holding page rather than ship below the bar. All January Zion-era decisions above (Svelte/GSAP/particles/WebGL) are now historical — site is Next.js 16 static export with Inter only. | Martin | — |
| 2026-06-08 | Authorize portfolio expansion; establish `design/BRIEF.md` as design north-star | Re-opening expansion with explicit setup: refreshed memory, project-scoped `.claude/settings.json`, `design-critic` subagent, archived stale `PROJECT_START.md`, rewrote `README.md`. Visual direction itself remains open — captured in `design/BRIEF.md §6`. | Martin / Claude Code | — |
| 2026-06-08 | Design system codename "Zion" — pure white + ink hairlines + Inter / IBM Plex Mono | Closes BRIEF §6 open questions on palette, type, and motion. Pure black/white, no chroma. 1px ink hairlines are the signature; greyscale ramp for hierarchy only. Type pairing: Inter (display + body) + IBM Plex Mono (data/chrome). Motion limited to 80/160/240/400ms with `--ease-out` default. Full spec in `design/SYSTEM.md`; tokens in `site/styles/tokens.css`. Applied lightly to home button + contact form as initial demo. | Martin / Claude Code | — |
| 2026-06-08 | Add `/system` route as internal design-system reference page | Single-page demo of every primitive (colour ramp, type scale, type pairing, space, rules, buttons × 3 variants × 4 states, fields, links, dividers, coord labels, frame, status line, cursor) plus patterns (section identifier, asymmetric two-up) and anti-patterns. `noindex` via page metadata so it doesn't get crawled. Lives at `/system`; not linked from public navigation. | Martin / Claude Code | — |
| 2026-06-08 | Add entrance motion library (5 utilities + 5 stagger steps) | Extends SYSTEM.md §7 with an "entrance" motion domain alongside the existing "state" domain. Utilities: `.reveal-rise`, `.reveal-fade`, `.reveal-mask`, `.reveal-line`, `.reveal-clip` — all CSS-only, run once on mount, no scroll triggers. New tokens: `--dur-reveal` (700ms), `--dur-line` (900ms), `--ease-reveal` (cubic-bezier 0.16, 1, 0.3, 1), `--stagger-1..5`. Applied to `/` (crest + button), `/contact` (hero + form), `/system` (title + dedicated motion block with replay). Reduced-motion zeros everything. | Martin / Claude Code | — |
| 2026-06-08 | Wire `/contact` form via `mailto:` (no third-party form service) | Discovered the existing submit handler was a `setTimeout` stub — no email was being sent locally or in production. Chose option A (mailto) over option B (Vercel function + SMTP) to keep zero infrastructure and stay on static export. Submit handler now builds a `mailto:hello@martindrexler.com?subject=...&body=...` URL from form fields and triggers it via `window.location.href`. Success state replaced with honest "MAIL APP SHOULD HAVE OPENED" copy + fallback link to the address directly. Archived stale `site/FORM_SETUP.md` (Web3Forms plan) to `docs/archive/`; removed unused `site/.env.example`. | Martin / Claude Code | — |
| 2026-06-08 | Switch from `mailto:` to a real serverless contact endpoint (option B) | `mailto:` doesn't fire reliably on browser-only Chromebooks / privacy modes / sandboxed corporate browsers. Replaced with a Vercel serverless function at `/api/contact` that POSTs through Google Workspace SMTP using `nodemailer`. Dropped `output: 'export'` from `next.config.ts` — site is now a hybrid Next.js deploy (static pages still edge-cached, only `/api/contact` invokes a function). Removed root `vercel.json`; deploy now requires the Vercel project's Root Directory to be set to `site/`. Six env vars required (`SMTP_HOST/PORT/USER/PASS`, `MAIL_FROM`, `MAIL_TO`) in both Vercel and `.env.local`. Honeypot field (`company_url`) added to the form to filter bots; min/max length validation server-side. | Martin / Claude Code | — |
| 2026-06-09 | Replace holding page with WebGL fluid landing (crest warped by velocity field) | Reference research on storytelling.noomoagency.com identified a Pavel Dobryakov fluid simulation as the wow-effect technique that translates cleanly to Zion v2 (palette-agnostic — render ink density as black on white instead of coloured smoke). Built from scratch in raw WebGL2 — no Three.js, no OGL, no GSAP, zero new dependencies. Crest is sampled inside the display shader with UVs warped by the velocity field, so the mouse disturbs both the ink trails and the crest itself; warp envelope decays exponentially (1.0s half-life) and only reactivates when a pointer move lands inside the crest's bounding box. Four-corner chrome: `MD_DESIGN` (top-left), `Contact` (top-right), `Martin Drexler` (bottom-left), `Design Studio` (bottom-right). `prefers-reduced-motion` skips the canvas entirely. Files: `site/app/{page,FluidCanvas,fluid,shaders}.{tsx,ts}`. | Martin / Claude Code | — |
