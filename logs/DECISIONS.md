# Decision Log

| Date | Decision | Rationale | Owner | Ticket |
| --- | --- | --- | --- | --- |
| 2026-01-09 | Zion UI aesthetic clinical/technical feeling is brand-aligned and intentional | Client confirmed that clinical, stark aesthetic perfectly aligns with data-driven efficiency and results philosophy. The technical appearance expresses core brand values. | Client / Implementation Engineer | Research Phase |
| 2026-01-09 | Portfolio content will be primarily image-based | Images work well with minimal aesthetic. Not a concern for content integration. | Client | Research Phase |
| 2026-01-09 | Symbol-based navigation accessibility to be handled by client | Client will manage strategy for symbol-based navigation accessibility. Implementation Engineer aware but not responsible for this aspect. | Client | Research Phase |
| 2026-01-09 | Particle effects are a core requirement for the UI system | Client specified that particle effects must be integrated into the design system as a primary feature, not optional enhancement. | Client / Implementation Engineer | Research Phase |
| 2026-01-09 | Recommended tech stack: Svelte + GSAP + tsParticles + Vite | Based on performance research, Svelte offers best performance (3x faster updates than React), GSAP provides industry-standard animation capabilities ideal for SVG line work, tsParticles offers highly customizable particle effects with framework support, and Vite provides fastest build tooling. Alternative: React + Framer Motion for ecosystem maturity. | Implementation Engineer | Research Phase |
| 2026-01-09 | WebGL-based rendering for particle effects | Research shows WebGL significantly outperforms Canvas 2D for 1000+ particles and enables advanced holographic effects. tsParticles supports WebGL mode, PixiJS for extreme performance needs (100K+ particles), Three.js for true 3D holographic depth. | Implementation Engineer | Research Phase |
| 2026-04-29 | Scrap all expansion; reduce to holding page (crest + Contact button) | Multiple visual directions had drifted into generic territory; resetting to a holding page rather than ship below the bar. All January Zion-era decisions above (Svelte/GSAP/particles/WebGL) are now historical — site is Next.js 16 static export with Inter only. | Martin | — |
| 2026-06-08 | Authorize portfolio expansion; establish `design/BRIEF.md` as design north-star | Re-opening expansion with explicit setup: refreshed memory, project-scoped `.claude/settings.json`, `design-critic` subagent, archived stale `PROJECT_START.md`, rewrote `README.md`. Visual direction itself remains open — captured in `design/BRIEF.md §6`. | Martin / Claude Code | — |
| 2026-06-08 | Design system codename "Zion" — pure white + ink hairlines + Inter / IBM Plex Mono | Closes BRIEF §6 open questions on palette, type, and motion. Pure black/white, no chroma. 1px ink hairlines are the signature; greyscale ramp for hierarchy only. Type pairing: Inter (display + body) + IBM Plex Mono (data/chrome). Motion limited to 80/160/240/400ms with `--ease-out` default. Full spec in `design/SYSTEM.md`; tokens in `site/styles/tokens.css`. Applied lightly to home button + contact form as initial demo. | Martin / Claude Code | — |
| 2026-06-08 | Add `/system` route as internal design-system reference page | Single-page demo of every primitive (colour ramp, type scale, type pairing, space, rules, buttons × 3 variants × 4 states, fields, links, dividers, coord labels, frame, status line, cursor) plus patterns (section identifier, asymmetric two-up) and anti-patterns. `noindex` via page metadata so it doesn't get crawled. Lives at `/system`; not linked from public navigation. | Martin / Claude Code | — |
| 2026-06-08 | Add entrance motion library (5 utilities + 5 stagger steps) | Extends SYSTEM.md §7 with an "entrance" motion domain alongside the existing "state" domain. Utilities: `.reveal-rise`, `.reveal-fade`, `.reveal-mask`, `.reveal-line`, `.reveal-clip` — all CSS-only, run once on mount, no scroll triggers. New tokens: `--dur-reveal` (700ms), `--dur-line` (900ms), `--ease-reveal` (cubic-bezier 0.16, 1, 0.3, 1), `--stagger-1..5`. Applied to `/` (crest + button), `/contact` (hero + form), `/system` (title + dedicated motion block with replay). Reduced-motion zeros everything. | Martin / Claude Code | — |
| 2026-06-08 | Wire `/contact` form via `mailto:` (no third-party form service) | Discovered the existing submit handler was a `setTimeout` stub — no email was being sent locally or in production. Chose option A (mailto) over option B (Vercel function + SMTP) to keep zero infrastructure and stay on static export. Submit handler now builds a `mailto:hello@martindrexler.com?subject=...&body=...` URL from form fields and triggers it via `window.location.href`. Success state replaced with honest "MAIL APP SHOULD HAVE OPENED" copy + fallback link to the address directly. Archived stale `site/FORM_SETUP.md` (Web3Forms plan) to `docs/archive/`; removed unused `site/.env.example`. | Martin / Claude Code | — |
| 2026-06-08 | Switch from `mailto:` to a real serverless contact endpoint (option B) | `mailto:` doesn't fire reliably on browser-only Chromebooks / privacy modes / sandboxed corporate browsers. Replaced with a Vercel serverless function at `/api/contact` that POSTs through Google Workspace SMTP using `nodemailer`. Dropped `output: 'export'` from `next.config.ts` — site is now a hybrid Next.js deploy (static pages still edge-cached, only `/api/contact` invokes a function). Removed root `vercel.json`; deploy now requires the Vercel project's Root Directory to be set to `site/`. Six env vars required (`SMTP_HOST/PORT/USER/PASS`, `MAIL_FROM`, `MAIL_TO`) in both Vercel and `.env.local`. Honeypot field (`company_url`) added to the form to filter bots; min/max length validation server-side. | Martin / Claude Code | — |
| 2026-06-09 | Replace holding page with WebGL fluid landing (crest warped by velocity field) | Reference research on storytelling.noomoagency.com identified a Pavel Dobryakov fluid simulation as the wow-effect technique that translates cleanly to Zion v2 (palette-agnostic — render ink density as black on white instead of coloured smoke). Built from scratch in raw WebGL2 — no Three.js, no OGL, no GSAP, zero new dependencies. Crest is sampled inside the display shader with UVs warped by the velocity field, so the mouse disturbs both the ink trails and the crest itself; warp envelope decays exponentially (1.0s half-life) and only reactivates when a pointer move lands inside the crest's bounding box. Four-corner chrome: `MD_DESIGN` (top-left), `Contact` (top-right), `Martin Drexler` (bottom-left), `Design Studio` (bottom-right). `prefers-reduced-motion` skips the canvas entirely. Files: `site/app/{page,FluidCanvas,fluid,shaders}.{tsx,ts}`. | Martin / Claude Code | — |
| 2026-06-09 | Add awards strip to landing + restructure chrome (MD_DESIGN removed, Contact centered) | `awards-horizontal-black.png` (800×90) sampled as a second image layer inside the display shader with its own warp envelope and bounding-box hit-test, so hovering the awards strip warps it without disturbing the crest and vice versa. Layer order: paper → crest → awards → fluid ink. Refactored display shader to use a shared `sampleAsInk` helper and per-layer `uLogoDispStrength` / `uAwardsDispStrength` uniforms. Chrome restructured: removed `MD_DESIGN` top-left wordmark; moved the Contact button from top-right to centered below the awards via `top: calc(50% + 306px)` (desktop) / `+226px` (mobile, ≤768px). Bottom-left `Martin Drexler` and bottom-right `Design Studio` unchanged. | Martin / Claude Code | — |
| 2026-06-09 | /contact: fix descender clip + add 3D wireframe grid background + business brand mark | Three fixes shipped together. (1) `padding-bottom: 0.18em` on `.heroTitle` so the `g` descender in "Let's work together" stops getting cut by `reveal-mask`'s clip-path. (2) New `WireframeGrid.tsx` client component: Canvas 2D 3D lattice cube (4 subdivisions ≈ 300 edges) rotating slowly behind the form, 1px CSS-pixel lines with depth-based opacity 4–22 %, DPR-aware. Reduced-motion freezes the frame. Layered with `position: fixed; inset: 0; z-index: 0` and page content lifted to z-index 1 (page bg dropped, body white shows through). (3) Business brand mark (`assets/brand/logos/logo.jpg` copied to `site/public/images/logo.jpg`) added as a fixed top-left 56 px link to `/`, with `mix-blend-mode: multiply` so the JPEG square doesn't halo the wireframe. | Martin / Claude Code | — |
| 2026-06-09 | Center landing stack vertically + swap favicons to business logo | Landing: introduced `stackGeometry()` in `fluid.ts` to compute the total stack height (crest + 40 + awards + 32 + contact) from canvas dimensions; crest UV is shifted up by half the stack-below-crest height so the whole group (crest → awards → contact) sits centered on the viewport. The same maths publishes `--md-contact-top` on the document root via `publishStackVars()` on every resize, and the contact button reads it (`top: calc(50% + var(--md-contact-top, 225px))`) — single source of truth, no media-query duplication, scales to any viewport. New `cfg.contactGapPx` (32) and `cfg.contactHeightPx` (32) feed the calc. Favicons: replaced `site/app/icon.png` + `apple-icon.png` (crest derivatives) with `icon.jpg` + `apple-icon.jpg` sourced from `assets/brand/logos/logo.jpg`. Build confirmed routes `/icon.jpg` and `/apple-icon.jpg` resolve. | Martin / Claude Code | — |
| 2026-06-09 | Fix landing contact button horizontal centering (reveal-rise was overriding translateX) | The contact button had `transform: translateX(-50%)` for horizontal centering AND a `reveal-rise` reveal class whose `to { transform: translateY(0) }` overwrote the translateX, pinning the button's left edge to viewport center instead of centering the button itself. Split the concerns per the motion library's own rule ("never combine a reveal class on the same element as a transform/state"): new `.contactSlot` wrapper owns positioning (`position: absolute; top: calc(50% + var(--md-contact-top))`; `left: 50%; transform: translateX(-50%)`), while the link keeps `reveal-rise reveal-stagger-3` and visual styling. | Martin / Claude Code | — |
| 2026-06-09 | Restore crest colour on landing (alpha-composite, not ink) | The crest had been treated as ink (luminance × alpha → black darkening of paper) when it moved into the display shader. Martin asked for the source heraldic colours back. Added a `sampleAsColor` helper to `shaders.ts` that alpha-composites the texture's RGB over whatever's already on the page; the crest sampling switched to use it. The awards strip stays on `sampleAsInk` (it reads as part of the paper). Fluid ink trails still overlay everything, including the crest, which fits the "ink on paper" metaphor. Note: this introduces chroma into the landing, which the Zion v2 system normally forbids — Martin's explicit override. | Martin / Claude Code | — |
| 2026-06-09 | Scale up /contact wireframe grid + drop opacity so it doesn't fight the copy | Scale factor on the lattice cube bumped from `min(w, h) * 0.32` to `* 1.2`, taking the projected vertical extent from ~25% of viewport to ~93%. Anchored on `min(w, h)` so mobile portrait can't spill the cube out the sides at 45° rotation extremes. Opacity range tightened from `[0.04, 0.22]` to `[0.02, 0.08]` (`0.05 − depth × 0.02` clamped) — a ghost of the previous grid, far lines almost dissolve, near lines top out at 8 %. Form copy still reads cleanly. | Martin / Claude Code | — |
| 2026-06-09 | Strip /contact info column down to Email / LinkedIn / Location | Removed the `"Other ways to connect"` heading (`infoTitle`), the leading hairline above the first contact method, and the `Availability / Currently accepting new projects` block. Right column now reads Email → LinkedIn → Location with hairlines between contact methods only (no top/bottom box). Added `padding-top: 90px` on `.infoColumn` so Email lines up with the form's first input row (Name/Email) on desktop; mobile media query zeroes it so the stacked layout has no awkward gap. Also pruned unused `.infoTitle`, `.availabilityInfo`, `.availabilityLabel`, `.availabilityValue` CSS rules. | Martin / Claude Code | — |
| 2026-06-10 | Lock positioning — UI/UX-led, brand-fluent, "design as business strategy" | Earlier BRIEF §3 placeholder ("brand identity and editorial systems") undersold him: of 25+ international awards, ~18 are UI/UX-leaning (Shop Usability ×5, IWB Shop ×2, Swiss eCommerce ×2, PE Wire ×6, Adobe UI/Xd, German Digital Innovation). Teaching credential (Daniels College of Business + WTC Denver, 2020–2022) earns the right to bridge UX + brand + product under one "design as business strategy" frame. New canonical paragraph follows Paula Scher pattern — short sentences, one claim each, named projects (CEPRES $45T fintech, Keller Sports €100m commerce, YCA Olympic Movement). Used verbatim on `/studio`. Detail in `design/BRIEF.md §3`. | Martin / Claude Code | — |
| 2026-06-10 | Lock six case studies + display order + studio attribution rule | Six projects in display order on `/work`: 1. CEPRES (Martin Drexler Design Studio, UX flagship), 2. Keller Sports (in-house Head of Design, most-awarded — 13), 3. YCA (Goodvoice CD, Olympic), 4. Planetarie (Goodvoice CD, CPG + acquisition), 5. byte (Haimish, consumer recognition + 3D packaging), 6. Space for Curiosity (Haimish, conditional on creative-direction role confirmation — fallback USA Ultimate). Studio attribution rule: every case study carries `STUDIO — [name]` in metadata block; honest about Haimish/Goodvoice/in-house authorship without hiding or inflating. NDA blocklist: D&AD Yellow Pencil project, BMW, Red Bull — can be named in `/studio` client list, no case studies. Build order = display order, with CEPRES as case-study #1 (the template gets shaped against the hardest case). Stacy Cason testimonial verbatim on Planetarie page; no site-wide testimonial wall. Detail in `design/BRIEF.md §7`. | Martin / Claude Code | — |
| 2026-06-10 | Adopt "personal-site-as-software" thesis + four signature moves | Three parallel research streams (signature/spine moves, typography, motion craft) converged on the same 2026 cutting-edge frame: the senior designer portfolio reads as a working piece of software, not "minimalist designer page." For Martin specifically, this lands cleanly because he teaches design-as-business-strategy — the site IS the positioning made visible. Four moves: A) WebGL2 fluid sim on `/` (already shipped); B) View Transitions on `/work` ↔ `/work/[slug]` (Next.js 16 `experimental.viewTransition`, 400ms `--ease-out`, `view-transition-name` on hero+title); C) ⌘K command menu + keyboard hint chrome (cross-page navigation, scale-up 160ms open / opacity-only 80ms close); D) persistent mono chrome strip on every page (MD_DESIGN / version / commit short hash / ISO build date / scroll progress / ⌘K hint — 10px Plex Mono, ink-60). A and B sit on opposite layers (canvas under, route morph over); C and D are typographic. They compound, don't compete. Detail in `design/SYSTEM.md §14` (sections 14.1–14.4). | Martin / Claude Code | — |
| 2026-06-10 | Extend type system — Inter variable + Display 200 + wght 510 + slnt + ss03 + drop cap + German bilingual | Switch Inter from static weight array to variable with `axes: ['opsz', 'slnt']` in `next/font`. Plex Mono stays static (not variable on Google Fonts). Adds: (a) case-study hero project name set in Inter Display @ wght 200, opsz 144, tracking -0.04em, leading 0.92, clamp(72px, 12vw, 184px) — Pentagram-scale presence inside the locked family; (b) non-integer wght 510 for H1/H2/buttons (Linear's signature trick — looks subtly custom); (c) slnt -10° for `<em>` instead of italic-bold; (d) global OpenType features `ss03` (round quotes), `cv11` (single-story a), `calt`; mono adds `ss02 zero`; tabular contexts add `tnum`; (e) `hanging-punctuation: first allow-end last` site-wide; (f) drop cap on case-study lede (5.6em, Inter Display 200); (g) mono eyebrow label pattern (`CASE 03 — IDENTITY — 2024`, Plex Mono 12px UPPERCASE 0.08em); (h) German bilingual strategy via per-page `lang="de"`, `hyphens: auto` on `:lang(de)`, spaced en-dashes vs em-dashes per language. No language switcher in chrome. Detail in `design/SYSTEM.md §14.5–14.6`. | Martin / Claude Code | — |
| 2026-06-10 | Close BRIEF open questions — case-study template, loading language, navigation, footer, page transitions | Closes five open questions from `design/BRIEF.md §6`. (1) Case-study template: 8-section canonical structure (Header → Hero artifact → Context → Problem → 4–8 Design-decision vignettes → Outcome → Credits → Pager). Vignettes are load-bearing. Detail in `SYSTEM.md §14.9`. (2) Loading language: mono `[STATE]` labels only — `[LOADING]`, `[SENDING]`, `[SENT]`, `[COPIED]`, `[LOADING 01/06]`. No spinners ever. `SYSTEM.md §14.8`. (3) Navigation: ⌘K command menu replaces persistent rail. Detail in `SYSTEM.md §14.3`. (4) Footer: mono chrome strip (`SYSTEM.md §14.4`) serves both. (5) Page transitions: View Transitions API on `/work` ↔ `/work/[slug]` with `view-transition-name` on hero + title; root crossfade on all other route changes (160ms); disabled under reduced-motion. `SYSTEM.md §14.2 + §14.7`. Two questions remain open: photography treatment (decide before CEPRES ships) + image preparation pipeline. | Martin / Claude Code | — |
| 2026-06-10 | Photography treatment — option C, 1px ink hairline frame, no greyscale, no tint | Closes the photography open question from `BRIEF §6`. Every case-study image sits inside a 1px ink hairline frame (`var(--rule)`), no `--ink-05` tint, no greyscale, no shadow. Matches the hairline-is-signature rule of the system; keeps dashboard colours readable as data evidence; lightest possible chrome that still separates artifact from page. Reserve greyscale for one specific case study only if a project's brand clashes with the system. | Martin / Claude Code | — |
| 2026-06-10 | Case-study CSS extracted into shared module + Keller Sports built as case #2 | When the second case study arrives, refactor — not before. Moved per-page `app/work/cepres/page.module.css` → shared `components/CaseStudy/CaseStudy.module.css`; both `cepres/page.tsx` and new `keller-sports/page.tsx` import from there. Confirms the template is reusable across different story shapes (CEPRES UX-led vs Keller Sports brand+commerce+in-house). | Martin / Claude Code | — |
| 2026-06-10 | Design-critic round 2 fixes — drop cap scope, /studio H1, vignette alternation, command menu backdrop | Five corrections after critic pass on `/studio` + `⌘K` + Keller Sports. (1) Drop cap is **case-study exclusive** — removed `lede` global class from `/studio` lede; `SYSTEM §14.5` updated with explicit exclusion. (2) `/studio` H1 = `Martin Drexler` (not "Studio") — the name does the work, mono eyebrow reads `STUDIO`; `BRIEF §3` clarified. (3) Vignette alternation **dropped** — every vignette leads with text uniformly (mechanical alternation across 4+ vignettes read as pattern, not editing); `SYSTEM §14.9` updated. (4) Command menu backdrop = solid `var(--bg)` (was `color-mix` translucent veil — read as "stuck overlay"); `SYSTEM §14.3` updated. (5) Command menu item count locked at five for v1 (Toggle Motion deferred until reduced-motion override system exists); `SYSTEM §14.3` updated. Plus: `/studio` header restructured into 7/5 (title + lede on left, mono meta block on right) matching the case-study header pattern. Keller Sports awards line compressed from a 5-line interpunct wall to `13 international awards (2014–16)`. | Martin / Claude Code | — |
